from tkinter import *
from tkinter import ttk
import tkinter.font as f
from tkcalendar import DateEntry
from tkinter import messagebox
import mysql.connector as ms
import pandas as pd
from datetime import date
from datetime import datetime
import os
try:
    con=ms.connect(host="localhost",user="root",database="bankmgt")
except:
    print("Error: connection is unsuccessful")
else:
    print("Connection successful")

def submit(user_amount,current_balance,ub):
    ub.set(str(int(current_balance)-int(user_amount)))

def withdraww(acc,bal):
    pass

def withdraw(Us):
    f1=f.Font(family="Proxima Nova",size=18,weight="bold")
    f2=f.Font(family="Aerial",size=17,weight="bold")
    f3=f.Font(family="Aerial",size=30,weight="bold")
    f4=f.Font(family="Aerial",size=10)
    f5=f.Font(family="Aerial",size=21,weight="bold")
    f6=f.Font(family="Aerial",size=9,weight="bold")

    withdraw=Toplevel()
    frame4=Frame(withdraw,height=520,width=600,bd=10,background="#a10e38")
    frame4.pack(padx=0,pady=0)

    title=Label(frame4,text="BALANCE WITHDRAW",font=f5,background="#a10e38",fg="white")
    title.place(x=130,y=30)


    label11=Label(frame4,text="Current Balance",font=f2,background="#a10e38",fg="white")
    label11.place(x=20,y=120)

    
    cur= con.cursor()
    query= "select Amount from create_table where username=%s"
    cur.execute(query,(Us,))
    data=cur.fetchone()
    
    label16=Label(frame4,text=data[0],width=36,height=1,font=f6)
    label16.place(x=290,y=125)

    label12=Label(frame4,text="Amount",font=f2,background="#a10e38",fg="white")
    label12.place(x=20,y=180)

    Cb=IntVar()
    cb=Entry(frame4,textvariable=Cb,width=42)
    cb.place(x=290,y=185)

    Ub=StringVar()
    ub=Entry(frame4,textvariable=Ub,width=42)
    ub.place(x=290,y=315)

    label13=Label(frame4,text="Updated Balance",font=f2,background="#a10e38",fg="white")
    label13.place(x=20,y=309)
    
    label15=Label(frame4,text="Description",font=f2,background="#a10e38",fg="white")
    label15.place(x=20,y=242)
    
    des_text= Text(frame4,width= 28,height=2,font=2)
    des_text.place(x=290,y=245)

    submit_bt=Button(frame4,text="Submit",command=lambda : submit(Cb.get(),data[0],Ub),font=f1,width=36,relief="groove")
    submit_bt.place(x=20,y=370)

    withdraw_bt=Button(frame4,text="Withdraw",command=lambda:withdraww(Us,Cb,des_text),font=f1,width=36,relief="groove")
    withdraw_bt.place(x=20,y=430)

    image=PhotoImage(file="bank-removebg-preview.png")
    labell=Label(frame4,image=image,bg="#a10e38")
    labell.image=image
    labell.place(x=8,y=12)

    imagee=PhotoImage(file="bank-removebg-preview.png")
    labell=Label(frame4,image=imagee,bg="#a10e38")
    labell.image=imagee
    labell.place(x=460,y=12)

def withdraww(Us,Cb,des_text):
    description=des_text.get("1.0",END).strip()
    cur=con.cursor()
    query="update create_table set Amount=Amount-%s where Username=%s"
    amount=Cb.get()
    cur.execute(query,(amount,Us))
    con.commit()

    if os.path.exists(Us+".csv"):
        df_old=pd.read_csv(Us+".csv")
        sr=len(df_old)+1
    else:
        sr=1
    today = datetime.today().strftime("%d/%m/%Y")     
    data={

        "Sr. no":[sr],
        "Date":[today],
        "Type of Transaction":["Withdraw"],
        "Amount":[amount],
        "Description":[description]
        
    }

    df=pd.DataFrame(data)

    if os.path.exists(Us+".csv"):

        df.to_csv(Us+".csv",mode="a",header=False,index=False)

    else:

        df.to_csv(Us+".csv",index=False)

